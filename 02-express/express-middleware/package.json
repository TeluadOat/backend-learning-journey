{
  "name": "express-middleware",
  "version": "1.0.0",
  "description": "-- functions that run during the request/response lifecycle.\r -- have access to request and response objects\r -- can end HTTP request by sending baack response with respose like res.send()\r -- can be chained together by calling next()",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "express": "^5.2.1",
    "morgan": "^1.10.1"
  }
}
