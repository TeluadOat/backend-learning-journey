


## Express Middleware
-- functions that run during the request/response lifecycle.
-- have access to request and response objects
-- can end HTTP request by sending baack response with respose like res.send()
-- can be chained together by calling next()

--learnt about morgan npm package, which could be useful for debugging later on. it is a useful login tool